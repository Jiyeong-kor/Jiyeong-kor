name: ADsP 사이트 검증

on:
  pull_request:
    paths:
      - "docs/index.html"
      - "docs/sw.js"
      - "docs/adsp/**"
      - "apps/adsp-study/**"
      - "scripts/verify_adsp_site.py"
      - ".github/workflows/adsp-site-check.yml"
  push:
    branches:
      - main
    paths:
      - "docs/index.html"
      - "docs/sw.js"
      - "docs/adsp/**"
      - "apps/adsp-study/**"
      - "scripts/verify_adsp_site.py"
      - ".github/workflows/adsp-site-check.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: adsp-site-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: 저장소 체크아웃
        uses: actions/checkout@v4

      - name: Python 설정
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: 배포 자산 검증
        run: python scripts/verify_adsp_site.py
